{
    "name": "zhuiks-pages",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "dev": "next dev",
        "build": "next build && next export",
        "start": "next start",
        "test": "jest",
        "deploy": "yarn build && cp .nojekyll ./out && git add out/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix out origin gh-pages"
    },
    "dependencies": {
        "clsx": "^1.1.1",
        "gray-matter": "^4.0.2",
        "next": "^12.1.0",
        "react": "^17.0.1",
        "react-dom": "^17.0.1",
        "sass": "^1.29.0"
    },
    "devDependencies": {
        "@types/jest": "^26.0.15",
        "@types/node": "^14.11.8",
        "@types/react": "^16.9.56",
        "jest": "^26.6.0",
        "ts-jest": "^26.4.1",
        "typescript": "^4.0.3"
    }
}